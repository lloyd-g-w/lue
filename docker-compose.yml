services:
  server:
    build:
      context: .
      target: server-runtime
    environment:
      SERVER_ADDR: 0.0.0.0:3000
      DATA_PATH: /data/store.json
      SERVER_PUBLIC_BASE_URL: http://127.0.0.1:8081
      FRONTEND_BASE_URL: http://127.0.0.1:8081
    volumes:
      - lue-data:/data
    restart: unless-stopped

  web:
    build:
      context: .
      target: web-runtime
    depends_on:
      - server
    ports:
      - "8081:80"
    restart: unless-stopped

volumes:
  lue-data:
